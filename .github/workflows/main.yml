# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "master" branch
  push:
    branches: [ "master", "dev" ]
  pull_request:
    branches: [ "master" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  build:
    runs-on: ubuntu-latest
    if: github.ref_name == 'master'
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 16
      - name: Get version
        id: package-version
        run: |
          echo "Reading version from ./package.json"
          PACKAGE_VERSION=$(cat ./package.json | jq '.version' | tr -d '"')
          echo ::set-output name=current-version::$PACKAGE_VERSION
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: julianwerner
          password: dckr_pat_1OUT__NJyc3StMSjGR1gQt88Af4
      - name: Build <and Publish>
        run: |
          docker build -t robocup-backend:${{ steps.package-version.outputs.current-version }} .
          docker push /robocup-backend:${{ steps.package-version.outputs.current-version }}
            
          
    
